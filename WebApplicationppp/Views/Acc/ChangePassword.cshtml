<div class="row first-item">
    <div class="col-sm-12">
        <div class="well">
            <form class="form-horizontal" role="form" name="change_form" novalidate ng-hide="isLoading">
                <h2>Регистрация</h2>
                <br />
                <div class="form-group">
                    <label class="col-sm-3 control-label" for="inputOldPass">Потребителско име</label>
                    <div class="col-sm-9">
                        <input type="password" class="form-control" placeholder="Стара парола" id="inputOldPass" name="oldPass" data-ng-model="model.OldPassword" required autofocus />
                        <span class="text-bold text-danger"
                              ng-show="change_form.inputOldPass.$error.required && (!change_form.inputOldPass.$pristine || change_form.$submitted)">
                            Полето е задължително!
                        </span>
                        
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label" for="inputPass">Парола</label>
                    <div class="col-sm-9">
                        <input type="password" class="form-control" name="pass" id="inputPass" placeholder="Парола" data-ng-model="model.NewPassword" required />
                        <span class="text-bold text-danger"
                              ng-show="change_form.pass.$error.required && (!change_form.pass.$pristine || change_form.$submitted)">
                            Полето е задължително!
                        </span>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label" for="inputConPass">Повторете паролата</label>
                    <div class="col-sm-9">
                        <input type="password" class="form-control" id="inputConPass" placeholder="Потвърдете паролата" name="confirmPassword" data-ng-model="model.ConfirmPassword" required>
                        <span class="text-bold text-danger"
                              ng-show="change_form.confirmPassword.$error.required && (!change_form.confirmPassword.$pristine || change_form.$submitted)">
                            Полето е задължително!
                        </span>
                    </div>
                </div>
                <button class="btn btn-info" type="submit" data-ng-click="change()">Сменете паролата</button>
                <div data-ng-hide="message == ''" data-ng-class="(savedSuccessfully) ? 'alert alert-success' : 'alert alert-danger'">
                    {{message}}
                </div>
            </form>
            <div class="loading-indicator" ng-show="isLoading">
                <svg class="spinner" width="65px" height="65px" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                    <circle class="path" fill="none" stroke-width="6" stroke-linecap="round" cx="33" cy="33" r="30"></circle>
                </svg>
            </div>
        </div>
    </div>
</div>

